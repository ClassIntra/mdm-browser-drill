# MDM 浏览器层过滤器黑盒测绘 · 完整测试报告

> 两轮实测 · 62 探针 · 引擎判定规则收敛为 12 行伪代码 · 2026-09-08 至 2026-09-09
>
> 授权环境 · 自有设备 · 自有靶机 · 不 root / 不越狱 / 不装证书 / 不做中间人

**合规边界声明**：全部实验在实验者自有受管设备、自有靶机与授权网段内完成，探测目标只指向实验者自己的服务器地址；未触碰厂商云端、未对第三方设备发起任何请求、未植入持久化载荷。本报告仅用于授权环境内的技术学习与自有配置自查。

---

## 一、执行摘要

受管平板的 MDM 浏览器过滤器以「无效的响应」软拦内网裸 IP 访问。两轮共 **62 个探针**的黑盒测绘将引擎判定规则完整收敛（模型 v1→v6），核心结论：

> **引擎最终模型（v6）**：对 GURL 规范化后的 URL authority 拆为 `token（@ 前）`与 `host` 两段——**token 非空时只查 token**（含黑名单 IP 点分四段子串即拦，host 完全不被查）；token 空或无时才查 host。黑名单 = {192.168.40.1, 192.168.40.90, 192.168.125.1}，全端口、**纯字符串子串匹配**、大小写不敏感。32 个数据点零矛盾。

| 探针总数 | 受管设备 | 模型迭代 | 绕过原语 | 黑名单成员 |
|:---:|:---:|:---:|:---:|:---:|
| 62 | 2 | 6 | 2 | 3 |

**两类可复现绕过**：① 任意非空 userinfo token 短路（`http://a@192.168.40.90:9011/` 即达）；② IPv4-mapped IPv6 字面量使点分四段在规范化后消失（`http://[::ffff:192.168.40.90]:9011/` 即达）。另有域名形态完整解（nip.io 横线形 / 自有域名 A 记录）。

---

## 二、环境与方法

| 项目 | 第一轮（09-08） | 第二轮（09-09） |
|---|---|---|
| 受管设备 | CB-C8hPro-STU · Android 9 · X5 WebView TBS/046295 · Chrome/89（40.27） | CB-C6-STU · Android 9 · 系统 WebView · Chrome/80 无 X5（40.58） |
| 网络 | 192.168.40.0/24（MDM 管控）· 网关 192.168.40.1 · 课堂互动网关 192.168.125.1 | 同左 |
| 靶机 | 192.168.40.90:9011 / :18081，自研 Node 实验台（lab-server.mjs），逐请求留痕 access.log + observe.csv | 同左 |
| 探针 | N 组 7 · S 组 7 · D 组 3 · DN 组 13 = 30 | DP 组 32（case-dp2/case-dp3 页） |
| 窗口 | 20:50–21:45 | 08:37–11:38（跨内核复测） |

### 判读规则（本轮方法论核心）

- **「无效的响应」（秒出）= 引擎软拦**——合成响应，目标侧零痕迹；顶层导航与 XHR onerror 均属此列。
- **「连接被拒绝」/ 超时 / 出任何页面 = 引擎放行**——网络层真实结果。私网不可达目标转圈 30 秒以上，**转圈本身就是放行证据**。
- 每轮带阴性对照（DN1 随机串）；服务端 beacon 落账为准，屏幕文案只作旁证。

> 两台设备、两个内核（X5/89 与系统/80）全部结论一致 → 引擎位于 WebView 之下的 MDM 层，与内核无关。

---

## 三、引擎判定规则终稿（v6）

```python
BLACKLIST = {"192.168.40.1", "192.168.40.90", "192.168.125.1"}   # 纯子串匹配，全端口

def decide(authority: str) -> bool:
    # authority = GURL 规范化后的 "userinfo@host:port"（凭据未剥离）
    if authority.count("@") >= 2:
        return DENY                                        # N6：粗糙合法性检查

    token, _, hostport = authority.partition("@")

    if token == "":                                        # 空/无 userinfo → 查 host
        host = strip_port(hostport)
        return DENY if any(ip in host for ip in BLACKLIST) else ALLOW

    return DENY if any(ip in token for ip in BLACKLIST) else ALLOW
    # token 非空 → 只查 token，host 完全不被查（userinfo 旁路的真正机理）
```

### 逐条证据映射

| 规则 | 证据 | 结论 |
|---|---|---|
| 规范化输入 | DP1–DP4（十进制/十六进制/八进制/末尾点）全拦 | WHATWG URL 先规范化（quad 复原）再判定 |
| 规范化输入（续） | DP5 `[::ffff:192.168.40.90]` 过，location.host=[::ffff:c0a8:285a]:9011 | mapped-IPv6 被 hex 化，点分四段消失 → 分支不中 |
| token 短路 | N2 / DN1（任意串@IP 过）、DP7（token=1.2.3.4 过，02:53 复测）、**DP32（token="a" 过，64ms 到达）** | token 非空即只查 token，host 不设防 |
| token 段也查黑名单 | **DP29** token=192.168.40.90 + 域名 host → 拦 | 推翻 v4「任一段域名样即放行」 |
| 黑名单·纯子串 | **DP30** 192.168.40.100 拦（被 40.1 前缀连坐）；DN11/DN13 nip.io 点分形拦 | 非 IP 解析、非精确匹配，字面子串 |
| 黑名单·全端口 | .90 的 9011/18081 双拦（DP19）；.1 的 9011/80 双拦（DP26/27） | 端口不参与 |
| 黑名单·三成员 | DP26/27（.1）、N1/DP13/DP16/DP19（.90）、DP28（125.1）；DP31 portal 172.16.1.191 过 | 无第四成员；三员恰为网关+CI+课堂互动网关 |
| 黑名单独立于策略 siteList | 三条 /24 条目网络地址对账：119.6.237.1 过（DP17）/ 40.1 拦（DP26）/ 125.1 拦（DP28） | siteList「白名单」不参与浏览器层判定，两张表 |
| 黑名单外全放行 | DP20–25：同 /24 相邻、192.168/16、10/8、172.16/12、回环、公网 1.1.1.1 全过 | 无网段级守卫、无 RFC1918 整体封锁 |

---

## 四、模型演进史（六版迭代，每版被什么推翻）

| 版本 | 命题 | 被何推翻 / 确立 |
|---|---|---|
| v1 白名单整串包含 | authority 含放行域名即过 | DN1 阴性对照（zz-not-real.com@IP）放行 → 名单根本没被查 |
| v2/v3 字面量 IP 白名单 | token/host 任一段内嵌 quad → 精确查 IP 单，命中放行；CIDR 不展开 | DN7（40.1 拦）成立但语义当时不明；DN12 横线形过确立「quad 门控」 |
| v4 GURL 分段白名单 | 任一段不含 quad 即整体放行；两段都 IP 样逐段查表 | DP5（mapped-IPv6）确立输入=规范化后 authority；DP12/13/14/19 确立 302 豁免仅顶层、逐导航重判；DP17/25 公网放行埋下反转伏笔 |
| v5 黑名单 | 守卫=quad IP 黑名单字面匹配（token 域名样放行；host 查黑名单） | DP20–25（黑名单外全放行）+ DP26/27（.1 实锤）确立；「白名单查表」作废（DP17 过 vs DP26 拦，同 siteList 不同判） |
| **v6 token 短路（终稿）** | token 非空只查 token；token 空查 host；黑名单纯子串 | DP28（第三成员）、DP29（token 查黑名单）、DP30（子串连坐）、DP31（无第四成员）、DP32（非空即短路）终验，32 点零矛盾 |

> 方法论要点：每一版模型都死于一个精心设计的探针，而不是死于参数调优。**模型不是选出来的，是被一个个反例逼出来的。**

---

## 五、判定层事实（E1–E7，两轮通用）

| 编号 | 事实 | 关键证据 |
|---|---|---|
| E1 | 判定点在 WebView 请求拦截层，看到的是**凭据剥离前**的原始 URL（经 GURL 规范化） | S1/S2 对照：S2 页内 200 ↔ 日志唯一干净请求；S1 页内 404 ↔ 零命中 |
| E2 | 匹配对象=authority 段；大小写不敏感（N7）；端口无关（N3@18081）；query（N4）/fragment（N5）不参与 | N 组 |
| E3 | 拦截=合成响应软拦：顶层导航「无效的响应」、子资源合成 404——均不上线、零痕迹 | S 组 + 全部 DP 拦侧 |
| E4 | 放行后凭据剥离：线上请求全为干净形式；文档地址栏显示归一化形态 | DP32 REF=裸 IP 形态 |
| E5 | 通道差异：fetch 对 userinfo 构造期抛 TypeError；XHR/加载器剥离后照发（剥离前已过闸）；**明文 WS 保留凭据直达**（WS-UPGRADE 落账） | S3/S7、DP15 |
| E6 | 大文档无渲染阈值：8.2MB 单文档正常加载 | D 组 pad=8 |
| E7（二轮新增） | **逐导航重判、无会话信任**：服务端 302 豁免仅顶层导航（DP12 过）；JS 直跳（DP13）、XHR 中继（DP14）、到达裸 IP 页后再跳（DP19）全部重判拦；hex origin 页内相对请求全免检（DP18，REF=[::ffff:c0a8:285a] 实锤） | C 组 |

---

## 六、完整探针清单（62 项）

### 第一轮 · N 组（导航层，7）

| 编号 | 输入 | 结果 |
|---|---|---|
| N1 | `http://192.168.40.90:9011/` | 拦 · 零命中 |
| N2 | `http://spark.changyan.com@192.168.40.90:9011/` | 过 · 12:53:32 命中 |
| N3 | N2 换端口 18081 | 过 |
| N4/N5 | 放行域名放 query / fragment | 拦 · 零命中 |
| N6 | 双 `@` | 拦 |
| N7 | N2 全大写 | 过 |

### 第一轮 · S 组（子资源与协议，7）

| 编号 | 通道 | 结果 |
|---|---|---|
| S1 | XHR 干净 | 404 + 零命中（合成） |
| S2 | XHR userinfo | 200 上线（已剥离） |
| S3 | fetch userinfo | 构造期 TypeError，请求未发出 |
| S4/S5 | img / script userinfo | 加载器剥离 → 干净 IP → 拦 |
| S6 | userinfo base + 相对 XHR | 200 |
| S7 | WS userinfo | 握手成功 · WS-UPGRADE 落账 |

### 第一轮 · D 组（页面形态与稳健性，3）

三种页面形态 + 8.2MB 大文档全部一致；间隔 6–8 秒无一翻转 → **引擎确定性，无策略同步窗口**。

### 第一轮 · DN 组（匹配粒度，13）

| 编号 | 样本 | 结果 |
|---|---|---|
| DN1 | `zz-not-real.com@IP`（阴性对照） | 过 ← 名单没被查，全场最值钱一枪 |
| DN2–6, DN10 | 各域名串 / %2E 编码 | 全过 |
| DN8/DN9 | 单内明文 IP（119.6.237.1 / 223.5.5.5 作 token） | 过 |
| DN7 | token=192.168.40.1（策略写作 192.168.40.1/24） | 拦（二轮 DP26 补实锤语义） |
| DN11/DN13 | host=192.168.40.90.nip.io / .sslip.io（内嵌 quad） | 拦 · 零命中 |
| DN12 | host=192-168-40-90.nip.io（横线形） | 过 · 全链路可用 |

### 第二轮 · DP 组（模型收敛，32）

| 编号 | 输入 | 结果 | 贡献 |
|---|---|---|---|
| DP1 | `http://3232245850:9011/`（十进制） | 拦 | 规范化先于判定（WHATWG URL） |
| DP2 | `http://0xc0a8285a:9011/`（十六进制） | 拦 | 规范化先于判定（WHATWG URL） |
| DP3 | `http://0300.0250.050.0132:9011/`（八进制） | 拦 | 规范化先于判定（WHATWG URL） |
| DP4 | `http://192.168.40.90.:9011/`（末尾点） | 拦 | 规范化先于判定（WHATWG URL） |
| DP5 | `http://[::ffff:192.168.40.90]:9011/` | 过 | **决定性**：mapped-IPv6 hex 化消字面；host=[::ffff:c0a8:285a]:9011 |
| DP6 | 空@ + quad（首轮数据污染） | 作废 | 干净复测=DP16 |
| DP7 | token=1.2.3.4 + host=横线形 nip.io | 过 · 02:53:13 复测 | token 非黑名单 → host 被无视 |
| DP8 | 随机前缀 `p80134.192-168-40-90.nip.io` | 过 | 网络侧第二层对新域名无名单 |
| DP9 | POST 裸 IP | 拦 · 0 落账 | 方法与 GET 同判（首轮污染数据已剔除） |
| DP10 | 相对 POST | 200 | 同上 |
| DP11 | CORS 预检裸 IP | 拦 | 预检同判 |
| DP12 | 放行页 /redir 302 → 裸 IP 页 | 到达 | 服务端重定向豁免（仅顶层） |
| DP13 | JS location.href 直跳裸 IP | 拦 | 逐导航重判 |
| DP14 | XHR 经 302 中继到裸 IP | 拦（onerror） | 豁免不延及子资源 |
| DP15 | WS 经 `[::ffff:c0a8:285a]`:9011 | 握手成功 · WS-UPGRADE ×2 | hex 形态 WS 全通 |
| DP16 | 空@ + quad 干净复测 | 拦 | 空 token 不短路 → host 被查 |
| DP17 | host=119.6.237.1（siteList 内） | 过（连接被拒=真实网络错误） | siteList 与浏览器层判定无关 |
| DP18 | hex origin 页内相对 XHR | 200 落账 · REF=hex 页 | hex 全栈闭环 |
| DP19 | 302 到裸 IP 页 → JS 跳 :18081 | 拦 · 18081 零落账 | 无会话信任；.90 全端口实锤 |
| DP20 | 192.168.40.99（同 /24） | 过（快速网络错误） | 黑名单外全放行：无网段守卫 |
| DP21 | 192.168.1.1 | 过 | 黑名单外全放行：无网段守卫 |
| DP22 | 10.1.2.3 | 过 | 黑名单外全放行：无网段守卫 |
| DP23 | 172.16.5.4 | 过 | 黑名单外全放行：无网段守卫 |
| DP24 | 127.0.0.1 | 过 | 黑名单外全放行：无网段守卫 |
| DP25 | 1.1.1.1（公网） | 过（页面出内容） | 黑名单外全放行：无网段守卫 |
| DP26 | 192.168.40.1:9011 | 拦（无效的响应） | 黑名单第二成员实锤 |
| DP27 | 192.168.40.1:80 | 拦 | .1 全端口 |
| DP28 | 192.168.125.1:9011 | 拦 | 黑名单第三成员 |
| DP29 | `http://192.168.40.90@192-168-40-90.nip.io:9011/` | 拦 | **token 段也查黑名单** → v6 |
| DP30 | 192.168.40.100:9011 | 拦 | **纯子串匹配**（被 40.1 前缀连坐） |
| DP31 | 172.16.1.191（portal） | 过（长转圈 · 双 fired 29s · 零下文） | 无第四成员；DROP 型不可达 |
| DP32 | `http://a@192.168.40.90:9011/` | 到达 · 64ms 自动标绿 | **token 非空即短路**，v6 终验 |

判读口径：拦=「无效的响应」或零落账；过=落账/到达/真实网络错误。首轮 DP6 与 DP9/11 的二轮标记为污染数据，以干净复测为准，已在表中注明。

---

## 七、通道可达性矩阵

| 通道 | 裸 IP | 字母@IP（userinfo） | 域名（无 quad） | 域名（内嵌 quad） | hex origin 页内相对请求 |
|---|---|---|---|---|---|
| 顶层导航 | 拦 | 过 | 过 | 拦 | 过 |
| XHR | 拦 | 过 | 过 | 拦 | 过 |
| fetch | 拦 | 构造期抛错 | 过 | 拦 | 过 |
| img / script | 拦 | 拦（剥离后判定） | 过 | 拦 | 过 |
| WebSocket | 拦 | 过（凭据直达） | 过 | 拦 | 过（DP15） |
| 服务端 302 → 裸 IP | 到达（仅顶层） | JS 直跳 / XHR 中继 / 二次导航一律重判（E7） | 同左 | 同左 | 同左 |

---

## 八、可用方案现状（按推荐序）

| 方案 | 形态 | 状态 | 说明 |
|---|---|---|---|
| **A · 自有域名（正道终态）** | 自有域名 A 记录 → 40.90，管理端入 siteList | 推荐 | 全功能零 hack；域名串不含点分四段即可 |
| **B · nip.io 横线形（即刻可用）** | 书签 `http://192-168-40-90.nip.io:9001/`，WS `ws://192-168-40-90.nip.io:10001` | 可用 | fetch/绝对路径/子资源全原生；依赖外部公共 DNS 解析 |
| C · userinfo 形态（过渡） | `http://a@192.168.40.90:9001/`（任意非空 token） | 可用 | token 短路任意串成立（DP32）；代价：fetch 报废、img/script 需内联、刷新后归一化失效 |
| D · hex origin（实验性） | `[::ffff:192.168.40.90]` 导航 + 页内全相对路径 | 可用 | DP5/15/18 闭环；入口依赖 hex 字面量，地址栏形态怪异 |
| × · 40.100 别名 | 服务器网卡加 192.168.40.100 别名 | 判死 | DP30：黑名单子串连坐（40.1 是 40.100 前缀） |
| × · 管理端 CIDR | siteList 写 192.168.40.1/24 之类 | 无效 | siteList 不参与浏览器层判定；黑名单另一张表 |

---

## 九、网络侧第二层观测

- 域名形态（无 quad）导航全部出网成功（DN2–6、DN12、DP8 随机新域名）→ **第二层「域名白名单+默认拒绝」对浏览器会话内的新域名未设防**，或其白名单按 SNI/Host 匹配且动态学习在吸收。
- 热点对照（192.168.137.x 不受管网）全功能 → 管控面只在 40 网生效。
- 成因未定，是本次**唯一未收敛的外层未知**；B 方案稳定性与此相关（已有数日稳定实测窗口）。

---

## 十、事故与对策记录（对后续同类演习可复用）

| # | 事故 | 根因 | 对策 |
|---|---|---|---|
| 1 | 一整轮设备端数据沉默，差点误读「全部放行」 | userinfo 基址下 `fetch(相对路径)` 构造期抛错被 .catch 吞掉 | 回传一律改 XHR |
| 2 | 「页内 200、日志零命中」假象 | 同 URL XHR 内存缓存 | 探测 URL 加 `?r=` 随机数 |
| 3 | 探针页区域级回写丢失（表格行回退旧版） | 外部同步进程与本地编辑竞态 | 新探针页换新文件名；页面磁盘直读（lab-server 每请求 readFileSync） |
| 4 | 新探针页上线 20 分钟设备零请求（「没有啊」） | 总控台漏挂入口 | 新页面必须同步挂总控台入口 |
| 5 | 探针按钮行「上线」但设备看不到 | 同文件多处编辑漏发/并行写盘竞态覆盖 | 同文件多处编辑逐个串行提交，提交后服务端响应实测校验 |

---

## 十一、遗留未解

- **黑名单来源**：{40.1, 40.90, 125.1} 恰为网关+CI+课堂互动网关，疑似管理端「浏览器禁止站点」手工清单——待管理台审计闭环（用户侧动作）。
- **token 侧子串细节**：quad 嵌于长 token（如 `x192.168.40.90y`）是否同拦未单测；token 是否有 quad 门控（先判「含 quad」再查黑名单）未分辨——host 侧已确证两步语义（DN12 无 quad 直通），token 侧按对称假设处理。
- **IDN / Punycode**：只测了 %2E，Unicode 同形字与 Punycode 未测。
- **双 @ 拒绝的内部机理**：合法性检查位置与实现未知。
- **第二层成因**：见 §九。
- **快照时效**：结论对本次策略快照与两台设备内核成立；策略或引擎升级后应抽检复测（DP5/29/32 三针即可回归）。

---

## 附录 A · 关键日志时间线摘录

```
── 第一轮（40.27 / X5）──
[12:53:32]        GET /case-nav.html?case=N2        200   N2 userinfo 导航（首破）
[12:54:35.774]    GET /api/ping                     200   S2 剥离后唯一上线请求
[12:54:35.813]    WS-UPGRADE /ws                          S7 明文 WS 直达

── 第二轮（40.58 / 系统 WebView）──
[00:37:54.280]    observe DP12 fired → /redir → case hit=1 落账    302 豁免
[01:31:45–01:32:00] DP14 blocked / DP15 allowed(WS-UPGRADE ×2) / DP16 blocked
[01:33:36]        GET /api/ping?case=DP18 200  REF=http://[::ffff:c0a8:285a]:9011/...   hex 全栈闭环
[02:26–02:27]     DP20–25 fired ×2 轮，全部零 marks             黑名单外全放行
[02:53:13]        case=DP7&v=1 到达 + allowed                    token 分支复测
[03:05:05/03:05:12] DP28 / DP29 fired → 屏「无效的响应」         第三成员 + token 查黑名单
[03:35:35]        DP30 fired → 「无效的响应」                    子串连坐
[03:35:42+03:36:11] DP31 fired ×2（29s 重试）零下文 → 长转圈      portal 放行
[03:36:58.759]    DP32 fired → GET /case-dp3.html?case=DP32&v=1 200
[03:36:58.823]    DP32 allowed（到达后 64ms 自动标绿）           v6 终验
```

## 附录 B · 留痕文件清单

| 文件 | 内容 |
|---|---|
| `records/access.log` | 靶机逐请求原始日志（两端口、含 UA/REF/SF） |
| `records/observe.csv` | 设备端 beacon 结构化回传（fired/allowed/blocked） |
| `pages/case-nav/sub/dom/dp/dp2/dp3.html + index.html` | 全部探针页与总控台 |
| `server/lab-server.mjs` | 实验台：磁盘直读、/redir 与 /redir-xhr 中继、双端口 |
| `01-演习方案.html / 02-首轮实测结论.html` | 方案与第一轮结论 |
| `docs/文章-让软破再次伟大.md` | 过程叙事版（已同步 v6 终稿） |

---

*报告版本 v1.0 · 2026-09-09 · 与 ebmd-filter-model.md §二十一/§二十二 同源 · 本 md 版与 `03-完整测试报告.html` 内容同源*
